# 损坏的BLF测试文件说明

## 概述

`test_corrupted.blf` 是一个专门用于测试BLF文件解析错误处理功能的测试文件。该文件包含了有效的BLF数据以及在文件尾部故意插入的多种损坏数据，用于验证应用程序的部分解析能力。

## 文件结构

### 有效部分
- **20个CAN消息**：前20个消息都是完整且有效的CAN消息
  - 消息ID范围：0x100 - 0x113
  - 每个消息包含8字节数据
  - 时间戳递增（每条消息增加1,000,000纳秒）

### 损坏部分（文件尾部）

文件包含4种不同类型的损坏数据，用于测试不同的错误场景：

#### 1. 不完整的对象头
```
0x4A424F  (只有部分 "LOBJ" 签名，缺少最后一个字节)
```
**预期错误**：`Invalid LOBJ container magic string`

#### 2. 错误的magic number
```
0xDEADBEEF (无效的签名)
```
**预期错误**：`Invalid LOBJ container magic string`

#### 3. 对象大小声明过大
```
0x4A424F4C (正确的 "LOBJ" 签名)
object_size = 999999 (声明远大于实际可用数据)
```
**预期错误**：`Unexpected end of file`

#### 4. 随机垃圾数据
```
0xFF, 0xAA, 0x55, 0x00, 0x12, 0x34, 0x56, 0x78
```
**预期错误**：解析器会尝试读取但失败

## 使用方法

### 方法1：通过命令行测试

```bash
cd src/blf
cargo run --bin test_corrupted
```

### 方法2：通过GUI应用测试

1. 运行 canview 应用程序
2. 点击顶部菜单栏的 **"Open BLF"** 按钮
3. 选择 `test_corrupted.blf` 文件
4. 观察以下内容：

## 预期行为

### ✅ 成功部分
- **Logs视图**：显示20个成功解析的CAN消息
- **折线图**：可以正常绘制这20个消息的数据
- **消息列表**：在主视图中可以看到所有有效的CAN消息

### ⚠️ 错误部分
- **右下角状态栏**：显示类似以下信息：
  ```
  BLF 解析完成: 20 对象成功 | 3 个错误 (首个: Invalid LOBJ container magic string)
  ```

- **控制台输出**：显示详细的错误信息：
  ```
  ⚠️  BLF 解析过程中发现 3 个错误:
    错误 1: Invalid LOBJ container magic string (expected 0x4A424F4C)
    错误 2: Invalid LOBJ container magic string (expected 0x4A424F4C)
    错误 3: Unexpected end of file while parsing
    ✅ 但仍成功解析了 20 个对象，这些对象将正常显示
  ```

## 验证功能

此测试文件用于验证以下功能：

1. ✅ **部分解析能力**：即使文件有错误，也能解析出有效的数据
2. ✅ **错误报告**：错误信息清晰地显示在状态栏
3. ✅ **数据完整性**：正确的数据不受错误影响，正常显示在logs和图表中
4. ✅ **用户体验**：用户能够看到成功解析的数据，同时了解存在的错误

## 重新生成测试文件

如果需要重新生成测试文件：

```bash
cd src/blf
cargo run --bin gen_corrupted_blf
```

这将在 `src/blf/` 目录下生成新的 `test_corrupted.blf` 文件。

## 技术细节

### 文件统计
- **应用版本**：1.0.0
- **文件大小**：~1.2KB
- **对象计数**：20个CAN消息
- **测量开始时间**：2025-01-15 14:30:00
- **压缩方式**：无压缩

### BLF格式
- **文件签名**：`0x47474F4C` ("LOGG")
- **对象签名**：`0x4A424F4C` ("LOBJ")
- **统计头大小**：144字节
- **头版本**：1

## 故障排除

### 如果没有看到任何数据
- 检查文件路径是否正确
- 确认文件的完整性和大小（应该约为1.2KB）
- 查看控制台输出是否有其他错误信息

### 如果没有看到错误信息
- 确认错误处理代码已正确实现
- 检查状态栏是否被其他内容遮挡
- 查看控制台输出是否有详细错误信息

## 相关文件

- `src/blf/src/bin/gen_corrupted_blf.rs` - 生成测试文件的源代码
- `src/blf/src/bin/test_corrupted.rs` - 测试解析的命令行工具
- `src/view/src/app/impls.rs` - 包含错误处理逻辑的应用代码

## 贡献

如果需要添加更多类型的损坏数据用于测试，请编辑 `gen_corrupted_blf.rs` 文件，并在损坏数据部分添加新的测试用例。