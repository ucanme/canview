# 信号波形图功能使用说明

## 功能概述

CANVIEW 现在支持将选中的 CAN/LIN 信号绘制成折线图，用于可视化分析信号的时间序列变化。

## 使用步骤

### 1. 选择 DBC/LDF 版本

1. 点击顶部菜单栏的 **"Library"** 按钮，进入信号库视图
2. 在左侧库列表中选择您需要的信号库
3. 在展开的版本列表中选择合适的版本
4. 在右侧的数据库预览区域会显示该版本的所有信号

### 2. 选择信号

1. 在数据库预览区域，浏览 Message 和 Signal 列表
2. 点击信号前面的复选框来选择需要分析的信号
   - 蓝色复选框：CAN 信号
   - 黄色复选框：LIN 信号
3. 顶部工具栏会显示当前选中的信号数量
4. 可以使用搜索框过滤信号

### 3. 加载 BLF 日志文件

1. 点击顶部菜单栏的 **"Open BLF"** 按钮
2. 选择您的 BLF 日志文件
3. 等待文件加载完成
4. 底部状态栏会显示加载的消息数量

### 4. 绘制波形图

有两种方式打开波形图：

**方式一：从信号库视图**
1. 在选择信号后，点击右上角的 **"Plot Selected"** 按钮
2. 系统会自动切换到 Plot 视图并生成波形图

**方式二：从顶部菜单**
1. 选择信号和加载 BLF 文件后
2. 直接点击顶部菜单栏的 **"Plot"** 按钮
3. 查看波形图

## 波形图特性

### 显示内容

- **多信号支持**：同时显示多个信号的波形，不同信号使用不同颜色
- **图例**：顶部显示每个信号的名称和颜色
- **网格线**：背景显示网格线便于读取数值
- **坐标轴**：
  - X 轴：时间（秒）
  - Y 轴：信号值（根据 DBC/LDF 定义解析后的物理值）

### 信号颜色

系统自动为每个信号分配颜色：
- 蓝色（Blue）
- 绿色（Green）  
- 橙色（Orange）
- 紫色（Purple）
- 红色（Red）
- 青色（Cyan）

### 性能优化

- 当数据点超过 5000 个时，系统会自动降采样以保证流畅性
- 空数据系列会被自动过滤

## 注意事项

1. **确保文件匹配**：选择的 DBC/LDF 版本应该与 BLF 日志文件中的 Message ID 匹配
2. **信号 ID 格式**：系统使用 `BusType:MessageID:SignalName` 格式标识信号
   - 示例：`CAN:123:EngineSpeed` 或 `LIN:16:VehicleSpeed`
3. **数据解析**：
   - CAN 信号根据 DBC 定义的 factor 和 offset 进行转换
   - LIN 信号根据 LDF 定义进行解析
4. **时间戳**：X 轴显示相对时间（从文件开始的秒数）

## 视图切换

- **Logs**：查看原始日志消息
- **Library**：管理信号库和选择信号
- **Plot**：查看波形图
- **Open BLF**：加载新的 BLF 日志文件

## 故障排除

### 波形图为空

- 检查是否已选择信号
- 检查是否已加载 BLF 文件
- 确认 BLF 文件中是否包含选中信号的数据

### 解析错误

- 确认 DBC/LDF 文件格式正确
- 确认 Message ID 与日志文件匹配
- 检查 Signal 定义是否完整

## 示例工作流程

1. 打开 CANVIEW
2. 点击 "Library" → 选择库 "Vehicle_Signals_v2.0"
3. 选择 3 个信号：
   - `CAN:123:EngineSpeed`
   - `CAN:456:VehicleSpeed`
   - `LIN:16:BatteryVoltage`
4. 点击 "Open BLF" → 选择 `test_drive.blf`
5. 点击 "Plot Selected" 按钮
6. 查看三条不同颜色的波形线，分析信号变化趋势

---

## 技术实现

本功能基于：
- **DBC 解析器**：解析 CAN 数据库文件
- **LDF 解析器**：解析 LIN 数据库文件  
- **BLF 读取器**：读取 Vector BLF 日志文件
- **GPUI 图表组件**：高性能的本地渲染
